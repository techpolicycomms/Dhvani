# dependencies
node_modules
/.pnp
.pnp.js

# testing
/coverage
/qa-report-screenshots/

# next.js
/.next/
/out/

# production
/build
/dist
# ...except the electron-builder resources directory (entitlements +
# icons). `/build` above would otherwise hide them.
!/build/
!/build/**

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env
.env.backup
.env.*.backup

# credentials & build artifacts (never ship these)
*.pem
*.key
*.cert
*.p12
*.pfx
dist/
dist-electron/
*.dmg
*.exe
*.blockmap

# user-data (transcripts, usage log, org-insights — may contain PII)
/data/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts.bak

# electron
/dist-electron
/electron/dist
/release
*.dmg
*.exe
*.blockmap

# server-side transcript persistence
/data

# python
__pycache__/
*.py[cod]
*$py.class
companion/venv/
companion/.venv/
graphify-out/

# Capacitor native project generated artifacts.
# The ios/ and android/ directories themselves ARE checked in
# (they contain project config like Info.plist, AndroidManifest,
# custom plugins), but the build outputs inside them should not be.
ios/App/Pods/
ios/App/build/
ios/DerivedData/
ios/App/public/
android/.gradle/
android/app/build/
android/build/
android/app/src/main/assets/public/
android/local.properties
android/capacitor.settings.gradle
*.xcuserstate
